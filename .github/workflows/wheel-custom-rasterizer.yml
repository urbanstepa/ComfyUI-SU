# Build custom_rasterizer CUDA wheel
# Trigger manually from Actions tab, or called by wheel-all.yml
name: "Wheel: custom_rasterizer"

on:
  workflow_dispatch:
  workflow_call:

env:
  TORCH_CUDA_ARCH_LIST: "7.5;8.0;8.6;8.9;9.0"
  FORCE_CUDA: "1"
  MAX_JOBS: "2"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:13.0.0-devel-ubuntu22.04

    steps:
      - name: Install Python 3.12 + pip
        run: |
          apt-get update -qq
          apt-get install -y --no-install-recommends \
            software-properties-common gpg-agent git
          add-apt-repository -y ppa:deadsnakes/ppa
          apt-get update -qq
          apt-get install -y --no-install-recommends \
            python3.12 python3.12-dev python3.12-venv
          update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.12 1
          update-alternatives --install /usr/bin/python python /usr/bin/python3.12 1
          python -m ensurepip --upgrade
          python -m pip install --upgrade pip setuptools wheel ninja

      - name: Install PyTorch 2.10.0+cu130
        run: |
          pip install torch==2.10.0+cu130 torchvision==0.25.0+cu130 torchaudio==2.10.0+cu130 \
            --index-url https://download.pytorch.org/whl/cu130

      - name: Clone source
        run: git clone --depth 1 https://github.com/urbanstepa/ComfyUI-Hunyuan3DWrapper.git /build/repo

      - name: Build wheel
        working-directory: /build/repo/hy3dgen/texgen/custom_rasterizer
        run: |
          pip wheel . --no-deps --wheel-dir /build/dist/ \
            || python setup.py bdist_wheel --dist-dir /build/dist/
          ls -lh /build/dist/*.whl

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: custom-rasterizer-wheel
          path: /build/dist/*.whl
          if-no-files-found: error
          retention-days: 90
