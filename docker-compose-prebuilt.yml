services:
  comfyui:
    image: comfyui-3d-prebuilt:latest
    container_name: comfyui-3d
    restart: unless-stopped

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    ports:
      - "8188:8188"

    volumes:
      # Adjust left side to your models path
      - "D:/ComfyUI_installed_2/models:/models"
      # Outputs and user data
      - comfyui_output:/opt/ComfyUI/output
      - comfyui_input:/opt/ComfyUI/input
      - comfyui_user:/opt/ComfyUI/user
      # NOTE: no comfyui_extensions volume — CUDA extensions are baked into the image

    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - COMFYUI_ARGS=--listen 0.0.0.0 --port 8188 --highvram

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8188/system_stats"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

volumes:
  comfyui_output:
  comfyui_input:
  comfyui_user:
